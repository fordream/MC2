a={}
i=1
function compile()
{
for file in `ls`;do
	if [ -d ${file} ];then
		cd $file
		compile
		cd ..
	elif [ ${file##*.} == "cpp" ];then
	 	a[$i]=`pwd`/${file}	
		let i=i+1
	fi
done
}
compile

j=$i;
i=1
while(( $i<$j ))
do
	g++ -g -O0 -c ${a[$i]};
	let i=i+1
done

g++ -g -O0 *.o -o mm
